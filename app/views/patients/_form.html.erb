<div class="container-fluid mt-3">
  <% if Current.user&.patient? %>
    <%# Keep back-button disable script for patients %>
    <script type="text/javascript">
      window.history.forward();
      function noback() { window.history.forward(); }
      window.onload = noback;
      window.onpageshow = function(evt) { if (evt.persisted) noback(); };
    </script>
    <%= render "patientConsentForm" %>
  <% else %>
    <%# --- MANAGER/ADMIN EDIT VIEW --- %>
    <h2 class="text-center mb-4">Edit Patient Record</h2>
    <%# We ONLY render the consent form, which now handles the buttons internally %>
    <%= render "patientConsentForm", manager_edit: true %>
  <% end %>
</div>